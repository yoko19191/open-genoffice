{
  "name": "@genoffice/ui",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Shared renderer UI components for the office apps (AI panel composer, icons); styling stays per-app via the shared class names",
  "exports": {
    ".": "./src/index.ts",
    "./agent-session-projection": "./src/agent-session-projection.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@vitest/coverage-v8": "^4.1.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@genoffice/agent-runtime-protocol": "*",
    "@genoffice/pptx-render": "*"
  }
}
