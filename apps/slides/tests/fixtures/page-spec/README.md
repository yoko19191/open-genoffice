# SlidePageSpec test vectors

These synthetic fixtures are maintained with Open GenOffice under the repository's Apache-2.0 license. `accepted/` contains valid version-1 inputs, `rejected/` isolates one forbidden condition per file, and `golden/structure.json` records the editable structure expected after local render, save and reopen.
