# 将 MCP 与 Subagent 作为一等产品能力

GenOffice 不把 MCP 与 Subagent 降格为“某个 Extension 恰好注册了一个工具”。
平台必须为它们提供可配置、可观察、可授权和可取消的生命周期，并在 Office UI
中呈现连接或委派状态；实现时优先复用 Pi Extension、Package 与现有 Subagent
生态，但产品契约由 GenOffice Agent Platform 负责。
