# 锁定 Slides 整页生成的首版质量门槛

Slides 整页生成只有同时满足以下条件才算成功：导出的 PPTX 可打开；文字对象保持可
编辑；页面元素无越界、无非预期重叠；图片资源完整；生成失败时原页保持不变。质量
检查在替换原页前完成，失败结果只能作为诊断或预览，不能留下半写入页面。

首版不以主观美学评分作为自动发布门槛；视觉风格评价通过人工黄金样例补充，但不能
替代上述结构性检查。
